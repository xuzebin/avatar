commandPort -n ":5056";

spaceLocator -n "lob";
spaceLocator -n "lib";
spaceLocator -n "rib";
spaceLocator -n "rob";
spaceLocator -n "ln";
spaceLocator -n "rn";
spaceLocator -n "ul";
spaceLocator -n "lm";
spaceLocator -n "rm";
spaceLocator -n "ll";

parentConstraint -mo lob leftouterbrow;
parentConstraint -mo lib leftinnerbrow;
parentConstraint -mo rib rightinnerbrow;
parentConstraint -mo rob rightouterbrow;
parentConstraint -mo ln leftnose;
parentConstraint -mo rn rightnose;
parentConstraint -mo lm leftmouth;
parentConstraint -mo ul upperlip;
parentConstraint -mo rm rightmouth;
parentConstraint -mo ll lowerlip;

currentTime 1;
setKeyframe lob lib rib rob ln rn lm ul rm ll;